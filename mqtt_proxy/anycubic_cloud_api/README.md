Coloque aqui o conteúdo da biblioteca `anycubic_cloud_api` utilizada pelo proxy.

Estrutura esperada (exemplo):
- anycubic_api.py
- models/
- resources/

Durante o build, o Dockerfile copiará esta pasta para `/opt/anycubic_cloud_api` dentro do container.
